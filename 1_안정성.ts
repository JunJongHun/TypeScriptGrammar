// 타스 변수 선언
// 변수명: 타입명 = 할당될 값; 

let myName: string = "jonghun";

myName = 1; // error

console.log(myName.toUpperCase());

// 타입에 맞지 않는 함수를 사용 할 경우
let num: number = 1;

num.toUpperCase(); // error

