// function sum(인자: 인자의 타입):함수의 리턴 타입 {
// }

function sum(x: number, y: number): number {
	return x + y;
}

// 팀원 1
sum(1, 2); // 숫자 2개 넣으면 숫자를 리턴해주는 함수구나!

// 팀원 2
sum(1); // 에러가 나네? 이렇게 쓰면 안되는구나!













