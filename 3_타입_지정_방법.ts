// 변수
// 변수명: 타입명 = 할당될 값;

let height: number = 178;

// 함수
// function sum(인자: 인자의 타입):함수의 리턴 타입 {}

function sumBasic(x: number, y: number): number {
	return x + y;
}

// 화살표 함수
const sumArrow = (x: number, y: number): number => {
	return x + y;
}

